import { SafePipeResourceUrlPipe } from './safe-pipe-resource-url.pipe';

describe('SafePipeResourceUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new SafePipeResourceUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
